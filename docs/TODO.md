- [ ] 缺少库存回滚机制
- [ ] 分布式锁添加重入机制
- [ ] 添加 `gateway` 服务提供统一 HTTP 入口
- [ ] 将分布式锁的创建从 `stock` 移动到 `order` 服务，并将 key 从订单中商品 id 改成 order_id，用于处理多实例场景下多个实例对同一个订单的减扣
- [ ] 库存减扣/确认时的幂等性：当 `stock` 完成减扣之后，因网络问题导致 `order` 没有实际得知库存状态，可能会重复减扣/确认（因为 `stock` 服务只是单纯的库存管理，没有订单相关元数据）
  - [ ] `order` 在调用 `stock` 前生成唯一的幂等键
  - [ ] `stock` 中添加新表，记录幂等键和返回值（减扣/确认状态），减扣/确认前使用幂等键确认是否已经做过这个操作，如果做过就读数据库，给之前的结果
  - [ ] gRPC client 增加重试和超时机制，重试时使用相同的幂等键
  - [ ] `stock` 中将库存预扣提取成单独的表，为预扣添加 ttl，创建定时任务清理超时库存占用
- [ ] 为 `order` 添加超时机制，订单创建成功之后通过延迟队列实现过期后的订单取消
- [ ] 提供补偿机制实现 SAGA 事务